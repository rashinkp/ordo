# Server configuration
PORT=4000

# MongoDB connection
MONGO_URI=mongodb://localhost:27017/rule_violation_tracker

# Frontend client URL
CLIENT_URL=http://localhost:5173

# JWT or session secrets (if needed later)
JWT_SECRET=your_jwt_secret_here

# Log level (info, warn, error, debug)
LOG_LEVEL=info
